name: Android Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_CLIENT_ID: ${{ secrets.GIT_GUI_OAUTH_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'
      - name: Validate GitHub OAuth configuration
        if: github.event_name != 'pull_request'
        run: |
          if [ -z "$GITHUB_CLIENT_ID" ]; then
            echo "::error::Missing repository secret GIT_GUI_OAUTH_CLIENT_ID"
            exit 1
          fi
      - name: Build debug APK
        run: |
          if [ -n "$GITHUB_CLIENT_ID" ]; then
            gradle :app:assembleDebug --stacktrace -PGITHUB_CLIENT_ID="$GITHUB_CLIENT_ID"
          else
            gradle :app:assembleDebug --stacktrace
          fi
      - uses: actions/upload-artifact@v4
        if: env.GITHUB_CLIENT_ID != ''
        with:
          name: GIT-GUI-debug
          path: app/build/outputs/apk/debug/app-debug.apk
